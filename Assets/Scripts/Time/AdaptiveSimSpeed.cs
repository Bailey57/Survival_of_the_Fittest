using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AdaptiveSimSpeed : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

        
        
    }

    /**
     * Sets the current timescale based on fps
     * 
     */
    public void GetTimeScale() 
    {
        //check the fps
        //x = fps
        //if >= x : increase timescale
        //else if < x : decrease timescale
        //else if in +- 2 fps of target: leave timescale alone
    }
}
